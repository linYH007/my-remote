{
  "name": "yuan-cheng-cloud-signaling",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "远程控制 - 云端信令中转服务器（房间配对 + SDP/ICE 转发 + 中继回退）。部署到任意 Node 平台即可获得固定公网地址。",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
